/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "img.clerk.com",
      "files.edgestore.dev",
      "picsum.photos",
      "i.pravatar.cc",
    ],
  },
};

export default nextConfig;
